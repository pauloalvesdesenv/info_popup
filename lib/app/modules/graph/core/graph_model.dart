import 'package:flutter/material.dart';

class GraphModel {
  final String label;
  final double length;
  final double vol;
  final Color color;
  GraphModel({
    required this.label,
    required this.length,
    required this.vol,
    required this.color,
  });
}
